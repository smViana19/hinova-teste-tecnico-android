
# Modelos de IA utilizados: 
- Claude Sonnet 4.6, 
- Gemini Pro (Preview) 
- Copilot
- Gemini Flash 2.5


IAs integradas ao Android Studio foram utilizadas para acelerar boilerplates, reduzir o tempo e tirar algumas duvidas, onde respostas foram revisadas e conferidas


### Prompts gerados


- "A permissao do usuário para acesso da localizacao deve ser aceita quando abrir o app ou quando logar no contexto em que somente uma tela precisará dessa feature?"


- "No android, quando uma api retorna imagens em formato base64, a função para decodificar e converter e exibir a imagem pode ficar diretamente no composable?" 

- "Você foi designado a adicionar a serializacao de json para o melhor uso no codigo android vindo da api na classe de modelo #file:Workshop.kt onde ja esta mapeada com o data class pronto porem preciso de adicionar a anotacao @SerializedName esse é o json de retorno com as chaves nomeadas: "


- "Você foi designado a adicionar as labels e melhorar a visualização placeholders dos inputs, conforme ja está pronto, porem precisa alem do placeholder da label o texto de label acima dos inputs."


- "Na criação do formulário, onde já está pronto, termine de implementar as validações dos campos, para evitar que não fiquem nulos ou vazios, e mostre um alerta que foi enviado com sucesso ou em caso de falha mostre com erro, a implementação deve seguir o padrão da #file:LoginScreen.kt e não divergir do padrão do projeto e dos padrões de codigo em que utilizo"


- "A implementação da permissao de localizacao tanto a exata e a proxima estão implementadas, porem me explique em qual camada do projeto ficaria melhor utilizar essa implementação, e utilizando interfaces em qual camada eu implementaria a classe concreta"